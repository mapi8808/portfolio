<h2 class="marker">商品一覧</h2>
<%= paginate @products %> <!--リストが20超えたら、ページング起動-->
  <%= render 'shared/products', shop: @shop, products: @products%>
<%= link_to 'HOME画面に戻る', root_path, class:"btn btn-secondary" %> <!--top画面へ-->
