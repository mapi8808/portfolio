<!DOCTYPE html>
<html>
  <head>
    <title>Swefo♫</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <!--ログイン中なら上記。違うならelse以下を表示-->
  <body style="background-color: #FFFFAA;">
    <header>
      <%= render 'shared/header', shop: @shop, product: @product, genre: @genre %>
      <!--文章が長くなるので、headerを部分テンプレート化しました。-->
    </header>

    <main class="mb-auto">
      <%= yield %>
    </main>

    <footer>
      <%= render 'shared/footer' %>
      <!--文章が長くなるので、footerを部分テンプレート化しました-->
    </footer>
  </body>
</html>
